using UnityEngine;

public class Player_GroundStompState : Player_GroundedState
{
    public Player_GroundStompState(Player player, StateMachine stateMachine, string animBoolName) : base(player, stateMachine, animBoolName)
    {
    }
}
