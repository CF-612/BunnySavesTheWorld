using UnityEngine;

public class Player_DigState : Player_GroundedState
{
    public Player_DigState(Player player, StateMachine stateMachine, string animBoolName) : base(player, stateMachine, animBoolName)
    {
    }
}
