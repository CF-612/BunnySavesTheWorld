# 蓄力跳跃（Charged Jump）需求案

## 文档说明
本文档描述"蓄力跳跃"技能的功能需求，供 AI 助手理解现有代码结构后实现。

---

## 功能名称
蓄力跳跃（Charged Jump）—— 长按空格蓄力，松手后弹跳力增强的跳跃技能。

## 功能概述
玩家在地面**长按空格键**进入蓄力状态，期间播放 "CargedJump" 蓄力动画。松手后以大于普通跳跃的力度起跳，空中动画复用现有 `Jump/Air/Fall` BlendTree。该技能在**兔子小镇**习得，用于跨越宽大沟壑、登高平台、在树枝间移动。

**注意**：该技能获得前，长按空格等同于普通跳跃（无蓄力效果）。

---

## 功能需求

### 1. 核心功能
- [ ] **蓄力检测**：地面状态下，空格按下后短暂延时（`chargeThreshold`），若持续按住则进入蓄力状态；若在阈值前松手则执行普通跳跃
- [ ] **蓄力状态**：进入 `Player_ChargedJumpState`，播放 "CargedJump" 动画，玩家保持原地（可左右微调方向但不可移动）
- [ ] **蓄力进度**：蓄力时间从 0 线性增长至 `maxChargeTime`，进度 = `Clamp01(chargeTimer / maxChargeTime)`
- [ ] **松手起跳**：松手时跳跃力度 = `Lerp(JumpForce, maxChargeJumpForce, chargeProgress)`，水平初速沿用空中移动倍率
- [ ] **空中复用**：起跳后切换至 `Player_JumpState`，共享现有 `Jump/Air/Fall` BlendTree 动画，后续下落→着地逻辑不变
- [ ] **能力锁**：蓄力跳跃在"兔子小镇"习得前不可用；习得前长按=普通跳跃（或最长按视为普通跳跃）

### 2. UI/交互需求
- [ ] **蓄力反馈**：蓄力动画 "CargedJump" 持续播放（由 Animator 控制）；可选的视觉特效（如脚下的蓄力光圈、角色闪烁等）留作后续迭代
- [ ] **进度提示**（可选后续迭代）：蓄力进度条或角色缩放/颜色变化
- [ ] **与其他操作的互斥**：蓄力期间屏蔽啃咬、跺脚、刨坑、单向平台下落等操作；移动输入仅用于决定起跳水平方向，不产生位移

### 3. 数据需求
- [ ] **蓄力时间追踪**：`chargeTimer`（float）— 当前已蓄力秒数
- [ ] **蓄力进度**：`chargeProgress`（float, 0~1）— 用于计算跳跃力度和可能的特效
- [ ] **是否正在蓄力标记**：`isCharging`（bool）— 防止重复进入蓄力状态

---

## 技术实现要求

### 脚本结构

```
Player_ChargedJumpState.cs（新建）
├── 继承关系
│   └── 继承 Player_GroundedState（蓄力时角色仍在地面）
├── 参数配置
│   ├── chargeTimer (float)：蓄力计时器
│   └── maxChargeTime (float)：引用 player.maxChargeTime
├── 核心方法
│   ├── Enter()：初始化计时器，设置 chargeJump animBool=true，屏蔽移动
│   ├── Update()：累加计时器，检测松手→计算力度→起跳
│   └── Exit()：清理标记

Player.cs（修改）
├── 新增字段
│   ├── chargedJumpState：蓄力跳跃状态实例
│   ├── maxChargeJumpForce (float)：最大蓄力跳跃力度
│   ├── maxChargeTime (float)：蓄满所需时间（秒）
│   └── chargeThreshold (float)：判定"长按"的阈值（秒）
├── 修改方法
│   └── Awake()：实例化 chargedJumpState

Player_GroundedState.cs（修改）
├── 新增字段
│   ├── jumpPressTimer (float)：空格按下计时
│   └── isChargingJump (bool)：是否正在监测长按
├── 修改方法
│   └── Update()：替换原有的 WasPressedThisFrame 直接起跳逻辑，改为长按检测

Player_JumpState.cs（修改）
├── 新增字段
│   └── customJumpForce (float)：外部传入的自定义跳跃力度（-1=使用默认）
├── 新增方法
│   └── SetCustomJumpForce(float)：供 ChargedJumpState 调用
├── 修改方法
│   └── Enter()：优先使用 customJumpForce，用后重置为 -1
```

### 依赖关系
- `Player.cs`：持有所有状态的引用和参数配置
- `Player_GroundedState.cs`：蓄力检测的逻辑起点（idle/move 都继承自此）
- `Player_JumpState.cs`：蓄力起跳后复用其空中逻辑（SetVelocity + yVelocity同步 + 跳跃音效）
- `Player_AirState.cs`：空中水平移动与风力交互（由 JumpState.base.Update 调用）
- `Animator Controller`：需新增 `chargeJump` Bool 参数和 "Player_CargedJump" 状态
- `PlayerInputSet`：**无需修改**（复用现有 Jump Action，用 `IsPressed()`/`WasReleasedThisFrame()` 检测长按）

---

## 参数详细说明

| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `maxChargeJumpForce` | float | 25 | 蓄满时的跳跃力度（普通跳跃 JumpForce=15） |
| `maxChargeTime` | float | 1.0 | 蓄力到最大值所需秒数 |
| `chargeThreshold` | float | 0.1 | 判定"长按"的延时阈值。松手时间 < 此值则执行普通跳跃 |
| `chargeJump` | bool (Animator) | false | Animator 参数：进入/退出 Player_CargedJump 动画状态 |

---

## 行为描述

### 场景1：快速点击空格（普通跳跃——未蓄力）
1. 用户操作：快速轻点空格（按下+松手 < `chargeThreshold`，约0.1秒内）
2. 系统响应：
   - `GroundedState.Update()` 检测 `Jump.WasPressedThisFrame()` → 开始监测
   - 松手发生在阈值前 → `WasReleasedThisFrame()` 触发 → 直接 `ChangeState(jumpState)`
   - `JumpState.Enter()` 以默认 `JumpForce=15` 起跳
3. 预期结果：**与现有普通跳跃完全一致**，无任何可感知的延迟或动画变化

### 场景2：长按空格蓄力后起跳
1. 用户操作：按住空格超过 `chargeThreshold`（约0.1秒）
2. 系统响应：
   - `GroundedState.Update()` 检测到按住时间 ≥ `chargeThreshold`
   - → `ChangeState(chargedJumpState)`
   - `ChargedJumpState.Enter()`：`anim.SetBool("chargeJump", true)`，播放 CargedJump 动画
   - 持续按住期间：`chargeTimer` 累加，角色保持原地
3. 用户操作：松手
4. 系统响应：
   - `ChargedJumpState.Update()` 检测 `Jump.WasReleasedThisFrame()`
   - 计算 `force = Lerp(JumpForce, maxChargeJumpForce, chargeProgress)`
   - 调用 `jumpState.SetCustomJumpForce(force)` → `ChangeState(jumpState)`
   - `JumpState.Enter()` 使用自定义力度起跳，同时同步 `yVelocity` 动画参数
5. 预期结果：
   - 松手瞬间以增强力度起跳（力度与蓄力时间成正比）
   - 空中动画 = 现有 `Jump/Air/Fall` BlendTree（yVelocity > 0→Jump动画, < 0→Fall动画）
   - 后续下落、着地逻辑与普通跳跃完全相同

### 场景3：蓄满后继续按住
1. 用户操作：按住空格超过 `maxChargeTime`（如1秒），继续按住不放
2. 系统响应：
   - `chargeProgress` 被 `Clamp01` 限制为 1.0
   - 继续播放 CargedJump 动画，力度不会再增加
3. 用户操作：松手
4. 预期结果：以最大力度 `maxChargeJumpForce` 起跳

### 场景4：蓄力中角色从平台边缘掉落
1. 用户操作：按住空格蓄力时，角色走到平台边缘
2. 系统响应：
   - `isGround` 变为 false，`rb.linearVelocity.y` 变为负值（重力）
   - `GroundedState.Update()` 基类中的 `rb.linearVelocity.y < 0 && !isGround` 在下一帧生效
   - → `ChangeState(fallState)`，**蓄力自动取消**
3. 预期结果：角色正常下落，不再执行蓄力起跳

### 场景5：蓄力时按下其他操作键
1. 用户操作：蓄力期间按 F（啃咬）或 J（跺脚）等
2. 系统响应：`ChargedJumpState.Update()` 中忽略这些输入（不调用基类 `GroundedState.Update()` 中的其他操作检测）
3. 预期结果：其他操作被屏蔽，蓄力继续进行

---

## 边界条件
- **阈值边界**：松手时间恰好等于 `chargeThreshold` → 视为普通跳跃（`<` 判定）
- **蓄满上限**：`chargeProgress` 被 `Clamp01` 限制，不会超过 1.0
- **空中误触**：`ChargedJumpState` 继承 `GroundedState`，只在 `isGround` 时有效。角色离地后无法进入蓄力
- **与滑翔的共存**：地面长按→蓄力跳跃（`GroundedState`），空中长按→滑翔（`AirState`），两者互不干扰
- **与单向平台下落共存**：S 键（JumpDown）走 `TryDropThroughPlatform()`，与 Jump 是不同的 InputAction，互不冲突。若玩家跳跃松手时未达到阈值→正常跳，不会触发平台下落
- **连续跳跃**：蓄力跳跃落地后回到 `IdleState`，再次按空格重新走检测流程

---

## 注意事项

### 关键修复经验
- **`yVelocity` 同步问题**：`JumpState.Enter()` 中 `SetVelocity` 后必须立即调用 `anim.SetFloat("yVelocity", rb.linearVelocity.y)`，否则 BlendTree 会在首帧读到残值 0 而短暂显示 Air 动画。**蓄力起跳进入 JumpState 时同样适用此修复。**

### Animator Controller 改造
- 需新增 `chargeJump` Bool 参数和 `Player_CargedJump` 动画状态
- `Player_CargedJump` 的 Exit 过渡：`chargeJump == false` → Exit（ConditionMode: 2 = IfNot）
- Entry 过渡：`chargeJump == true` → `Player_CargedJump`
- **无需修改** `Jump/Air/Fall` BlendTree 和现有动画状态的过渡逻辑
- **注意**：Unity Animator Controller 的 Entry 过渡是 `AnimatorTransition`（!u!1109），不是 `AnimatorStateTransition`（!u!1101），与现有的 idle/move/jumpFall 等保持一致

### 能力锁集成
- 建议 `Player` 添加 `hasChargedJump` bool，习得前在 `GroundedState` 中直接走原有普通跳跃逻辑
- 蓄力跳跃在"兔子小镇"教学中解锁

### 代码改动最小化
- `PlayerInputSet` **无需修改**，复用现有 Jump Action
- 现有 `JumpState`、`FallState`、`AirState` 逻辑**基本不变**
- 主要修改集中在 `Player_GroundedState` 和新建的 `Player_ChargedJumpState`

---

## 参考代码

| 文件 | 作用 |
|------|------|
| `Assets/Resources/Scripts/Player/Player.cs` | 参数定义 + 状态实例化 |
| `Assets/Resources/Scripts/Player/PlayerStates/Player_GroundedState.cs` | 长按检测逻辑修改 |
| `Assets/Resources/Scripts/Player/PlayerStates/Player_JumpState.cs` | 支持自定义跳跃力度 |
| `Assets/Resources/Scripts/Player/PlayerStates/Player_AirState.cs` | 空中水平移动（复用） |
| `Assets/Resources/Scripts/Player/PlayerStates/Player_FallState.cs` | 下落检测（复用） |
| `Assets/Resources/Scripts/StateMachine/EntityState.cs` | 基类：Enter/Exit/Update 生命周期 |
| `Assets/Resources/Scripts/StateMachine/StateMachine.cs` | 状态切换：Exit→切换→Enter |
| `Assets/Resources/Animations/Player/Player.controller` | Animator Controller：需新增 chargeJump 参数和 CargedJump 状态 |
| `Assets/Resources/Animations/Player/Player_Jump.anim` | 现有跳跃动画（BlendTree混合用） |
